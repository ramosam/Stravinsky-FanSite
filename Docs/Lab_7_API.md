# **CS296 Web-Development**

### Lab 7 - API

APIs are now live on APIUserStories and APIComments

Patch was handled a little differently because I did not use a view model to encapsulate the data.
Instead, string parameters were passed in key value pairs.


Feb 21, 2020